name: example_firebase
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1
resolution: workspace

environment:
  sdk: '>=3.10.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  tracelet:
    path: ../packages/tracelet
  tracelet_firebase:
    path: ../packages/tracelet_firebase

  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
