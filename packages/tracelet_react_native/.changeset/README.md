# React Native Package — Changesets

This directory is managed by [@changesets/cli](https://github.com/changesets/changesets).

To add a changeset run `npx changeset` in the `packages/tracelet_react_native/` directory,
then follow the prompts. Changesets are consumed during `npx changeset version` which
bumps `package.json` and updates `CHANGELOG.md`.
