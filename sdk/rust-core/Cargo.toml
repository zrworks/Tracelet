[workspace]
members = [
    "core",
    "sync"
]
resolver = "2"
[profile.release]
opt-level = "s"
codegen-units = 1
# DO NOT use strip = true, as it strips symbols needed by uniffi-bindgen
strip = "debuginfo"
panic = "abort"
