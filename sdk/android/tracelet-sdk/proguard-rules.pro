# Tracelet SDK — Library ProGuard/R8 Rules
# Applied when the SDK module itself is built with minification enabled.
# Currently isMinifyEnabled = false, so these rules are informational.
# Consumer-facing rules are in consumer-rules.pro.
